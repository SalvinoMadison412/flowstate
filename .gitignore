/node_modules
/.next
/out
/build
.DS_Store
*.pem
.env
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo

# local editor / tooling
.claude/settings.local.json

# unrelated local data files (not part of the site)
/*.csv
/*.xlsx

# outreach tooling (local data pipeline, not part of the site)
/outreach/

# nested app deps
node_modules/

# strategy docs — never publish
council/
